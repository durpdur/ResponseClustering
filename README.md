# ResponseClustering
Research for LLM's response clustering

# Component Structure
App.jsx
- Navbar
- HeroSection
- ClusterGrid
    - Clusters
        - Hover Card

- Flow Graph

# TODO
- Add graph using "medoid_item"
    - Surround medoid item with "other_items" (See ELK Radial Library for React Flow)
- Each card shows coordinates of their respective graph in the "Flow Graph" component

# To Deploy
In 'client' run `npm run build` to build, then `npm run deploy` to deploy