# ResponseClustering
Research for LLM's response clustering

# To Deploy
In 'client' run `npm run build` to build, then `npm run deploy` to deploy

# Kanban Board
- Fix Search Feature
- Implement React Flow Flow Chart Visualizer
- Implement HoverCard feature (displays cluster info in basic text)